# Move the mouse quickly to see the difference between the current
# and previous position
draw <- function() {
    background(204)
    line(20, mouseY, 80, pmouseY)
    print(mouseY)
    print(":")
    print(pmouseY)
    print("\n")
}
