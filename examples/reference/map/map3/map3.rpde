settings <- function() {
    size(200, 200)
}

setup <- function() {
    noStroke()
}

draw <- function() {
    background(204)
    x1 = map(mouseX, 0, width, 50, 150)
    ellipse(x1, 75, 50, 50)
    x2 = map(mouseX, 0, width, 0, 200)
    ellipse(x2, 125, 50, 50)
}
