# point 2 https://processing.org/reference/point_.html

settings <- function() {
    size(100, 100, P3D)
}

draw <- function() {
    noSmooth()
    point(30, 20, -50)
    point(85, 20, -50)
    point(85, 75, -50)
    point(30, 75, -50)
}
