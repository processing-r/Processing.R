settings <- function() {
    size(100, 100, P3D)
}

draw <- function() {
    noFill()
    ortho(0, width, 0, height)  # same as ortho()
    translate(width/2, height/2, 0)
    rotateX(-PI/6)
    rotateY(PI/3)
    box(45)
}
