# sphere 1 https://processing.org/reference/sphere_.html

P3D <- "processing.opengl.PGraphics3D"

settings <- function() {
    size(100, 100, P3D)
}

setup <- function() {
    noLoop()
}

draw <- function() {
    noStroke()
    lights()
    translate(58, 48, 0)
    sphere(28)
}
