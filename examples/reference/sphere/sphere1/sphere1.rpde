P3D <- "processing.opengl.PGraphics3D"

settings <- function() {
    processing$size(100, 100, P3D)
}

setup <- function() {
    processing$noLoop()
}

draw <- function() {
    processing$noStroke()
    processing$lights()
    processing$translate(58, 48, 0)
    processing$sphere(28)
}