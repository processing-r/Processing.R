# beginShape 1 https://processing.org/reference/beginShape_.html
# Fails on endShape(CLOSE) argument

OPEN <- 1
CLOSE <- 2

processing$beginShape()
processing$vertex(30, 20)
processing$vertex(85, 20)
processing$vertex(85, 75)
processing$vertex(30, 75)
# processing$endShape(CLOSE)
processing$endShape()
