# Move the mouse left and right across the image to see the cursor
# change from a cross to a hand

draw <- function() {
    if (mouseX < 50) {
        cursor(CROSS)
    } else {
        cursor(HAND)
    }
}
